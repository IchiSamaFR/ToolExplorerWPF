<UserControl x:Class="ToolExplorerWPF.Views.Controls.PsychonautJournal.IngestionsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolExplorerWPF.Views.Controls.PsychonautJournal"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ItemsControl Grid.Row="1"
        ItemsSource="{Binding Ingestions, RelativeSource={RelativeSource AncestorType={x:Type local:IngestionsControl}}}"
        Padding="0,5"
        HorizontalContentAlignment="Stretch">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid Margin="4,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border CornerRadius="4"
                        Background="{Binding SubstanceColor, Converter={StaticResource SubstanceColorsConverter}}"/>
                    <TextBlock Text="{Binding SubstanceName}"
                        TextWrapping="Wrap"
                        Grid.Column="1"
                        Margin="4,0,0,0"
                        HorizontalAlignment="Left"/>
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
    </ItemsControl>
</UserControl>
